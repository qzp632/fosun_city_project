const getters = {
    indexData: state => state.indexConfig.indexData,
    CityNameList: state => state.allimgConfig.CityNameList,
    flagVal: state => state.flags.flagVal,
    pathkey: state => state.pathobj.pathkey,
}
export default getters